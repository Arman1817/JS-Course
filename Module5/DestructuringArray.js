// Array destructuring is a JavaScript expression that allows us to extract data from arrays, objects, and maps and set them into new, distinct variables.

let arr = ['Arman',':)',':0',':/']

let [a ,b ,c ,d]= arr // Putting arr values into new variables and calling them 
console.log(c)